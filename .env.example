# =============================================================================
# BeingAI Assistant — Repository reference (not loaded automatically)
# =============================================================================
# Copy the scoped templates instead:
#   backend/.env.example  → backend/.env
#   frontend/.env.example → frontend/.env.local
# =============================================================================

# Backend → see backend/.env.example
# Frontend → see frontend/.env.example

# Quick local setup checklist:
# 1. Copy both example files above
# 2. Set backend DATABASE_URL, CLERK_SECRET_KEY, and CLERK_JWT_ISSUER
# 3. Set frontend NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY and NEXT_PUBLIC_API_URL
# 4. Run: uvicorn in backend/, npm run dev in frontend/
